{% extends 'admin/layout.html.twig' %}

{% block title %}Message{% endblock %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Expéditeur</th>
                <td>{{ contact.name }}</td>
            </tr>
            <tr>
                <th>Adresse email</th>
                <td>{{ contact.email }}</td>
            </tr>
            <tr>
                <th>Objet</th>
                <td>{{ contact.title }}</td>
            </tr>
        </tbody>
    </table>

    <br>{{ contact.message }}

    <br><br><br>{{ include('contact/_delete_form.html.twig') }}
{% endblock %}
